language: node_js
node_js:
- "stable"
cache:
  directories:
  - node_modules
script:
- yarn build
before_deploy:
- openssl aes-256-cbc -K $encrypted_04cf818f5d75_key -iv $encrypted_04cf818f5d75_iv -in deploy_rsa.enc -out /tmp/deploy_rsa -d
- eval "$(ssh-agent -s)"
- chmod 600 /tmp/deploy_rsa
- ssh-add /tmp/deploy_rsa
deploy:
  skip_cleanup: true
  provider: script
  script: rsync -r --delete-after --quiet build root@128.199.212.96:/var/www/westay-react/
  on:
    branch: dev
addons:
  ssh_known_hosts: 128.199.212.96
branches:
  only:
  - master
